export type Laptop = {
    id: number,
    createdAt: string,
    updatedAt: string,
    title: string,
    category: string,
    brand: string,
    ram: number,
    storage: number,
    os: string,
    releaseYear: number,
    image: string
}